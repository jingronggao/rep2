# Generated by IBM TransformationAdvisor
# Tue Jan 15 11:07:00 EST 2019

apiVersion: v1
kind: Service
metadata:
  name: senderwar
  labels:
    app: senderwar
spec:
  ports:
    - port: 9080
  selector:
    app: senderwar
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: senderwar
  name: senderwar
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: senderwar
    spec:
      containers:
      - image: mycluster.icp:8500/default/senderwar:latest
        name: senderwar
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
